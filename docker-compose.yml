services:

  backend:
    build: ./backend

    ports:
      - "5016:8080"

  frontend:
    build: ./frontend

    ports:
      - "5173:5173"

    volumes:
      - ./frontend:/app
      - /app/node_modules

    depends_on:
      - backend